#include<string.h> 
void main()
{
char str[20],reverse_str[100], temp;
int i=0, j=0;
printf ("enters the string - ");
gets(str);
j=strlen(str)-1; 
while(i<j)
{
temp=str[j];
str[j]=str[i];
str[i]=temp;
i++;
j--;
}
printf("reverse string is %s",str);
getch();
}
