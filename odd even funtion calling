

#include <stdio.h>

int main()
{
    int n,x;
    int iseven(int);
    printf("enter number");
    scanf("%d",&n);
    x=iseven(n);
    if (x==1)
    printf("no is even",n);
    else
    printf("no is odd",n);
    return 0;
    
}
int iseven(int n)
{
    if (n%2==0)

    return 1;
    else
    return 0;
}
