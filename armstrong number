#include <stdio.h>

int main()
{
    int n,r,sum=0,count=0;
    printf("enter value");
scanf("%d",&n);
count=n;
while(n>0)
{
    r=n%10;
    sum=sum+(r*r*r);
    n=n/10;
}
if(count==sum)
printf("armstrong number");
else
printf("not armstrong number");
    return 0;
}
