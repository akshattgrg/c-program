#include <stdio.h>

void main()
{
    float a,b,c;
    float sum(float,float);
    printf("enter two no");
    scanf("%f%f",&a,&b);
    c=sum(a,b);
    printf("%f",c);
    return 0;
}
float sum(float a,float b)
{
    float x;
    x=a+b;
    return x;
}
